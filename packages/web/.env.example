# Environment Variables

# API base URL (optional - defaults to proxy /api in development)
VITE_API_URL=http://localhost:3001/api

# Development server port (optional - defaults to 5173)
VITE_PORT=5173

# Wait time alert thresholds (in minutes)
# Time before PENDING orders show warning indicator
VITE_PENDING_WARNING_MINUTES=10
# Time before PENDING orders show critical indicator
VITE_PENDING_CRITICAL_MINUTES=20
# Time before IN_PROGRESS orders show warning indicator
VITE_IN_PROGRESS_WARNING_MINUTES=30

# Restaurant name for printed kitchen tickets
VITE_RESTAURANT_NAME=RestaurantFlow
