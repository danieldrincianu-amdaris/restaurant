# Story 1.1: Project Setup & Health Check

## Status: Approved

## Story

**As a** developer,  
**I want** the project scaffolded with proper structure and tooling,  
**so that** I have a solid foundation for building features.

## Acceptance Criteria

1. Monorepo structure created with `packages/api`, `packages/web`, and `packages/shared` directories
2. TypeScript configured for all packages with strict mode enabled
3. ESLint and Prettier configured with consistent rules across packages
4. Package.json scripts for dev, build, lint, and test commands
5. Express API server runs on configurable port (default 3001)
6. Health check endpoint (`GET /api/health`) returns `{ status: "ok", timestamp: <ISO date> }`
7. Basic error handling middleware catches and formats errors as JSON
8. README with setup instructions (install, run, test)

## Tasks / Subtasks

- [ ] **Task 1: Initialize Monorepo Structure** (AC: 1)
  - [ ] Create root `package.json` with pnpm workspaces configuration
  - [ ] Create `pnpm-workspace.yaml` defining packages/*
  - [ ] Create directory structure: `packages/api`, `packages/web`, `packages/shared`
  - [ ] Initialize each package with its own `package.json`
  - [ ] Set package names: `@restaurant/api`, `@restaurant/web`, `@restaurant/shared`

- [ ] **Task 2: Configure TypeScript** (AC: 2)
  - [ ] Create root `tsconfig.base.json` with shared strict settings
  - [ ] Create `packages/api/tsconfig.json` extending base
  - [ ] Create `packages/web/tsconfig.json` extending base  
  - [ ] Create `packages/shared/tsconfig.json` extending base
  - [ ] Configure paths for shared package imports

- [ ] **Task 3: Configure ESLint and Prettier** (AC: 3)
  - [ ] Install eslint, prettier, and TypeScript ESLint plugins at root
  - [ ] Create root `.eslintrc.js` with TypeScript rules
  - [ ] Create root `.prettierrc` with consistent formatting
  - [ ] Add `.eslintignore` for node_modules, dist, coverage

- [ ] **Task 4: Configure Root Scripts** (AC: 4)
  - [ ] Add `dev` script to run all packages in development
  - [ ] Add `build` script to build all packages
  - [ ] Add `lint` script to lint all packages
  - [ ] Add `test` script to run all tests
  - [ ] Add `typecheck` script for TypeScript validation

- [ ] **Task 5: Setup Express API Server** (AC: 5, 6, 7)
  - [ ] Install Express, cors, and TypeScript dependencies in api package
  - [ ] Create `packages/api/src/index.ts` entry point
  - [ ] Create `packages/api/src/app.ts` with Express app setup
  - [ ] Create `packages/api/src/config/index.ts` for environment config (PORT default 3001)
  - [ ] Create `packages/api/src/routes/index.ts` route aggregator
  - [ ] Create `packages/api/src/routes/health.ts` with GET /api/health endpoint
  - [ ] Create `packages/api/src/middleware/error-handler.ts` 
  - [ ] Wire up error handler middleware

- [ ] **Task 6: Create Shared Package Foundation** (AC: 1)
  - [ ] Create `packages/shared/src/index.ts` barrel export
  - [ ] Create `packages/shared/src/types/index.ts` for type exports
  - [ ] Configure shared package to build as CommonJS + ESM

- [ ] **Task 7: Create README** (AC: 8)
  - [ ] Write README.md with project overview
  - [ ] Document prerequisites (Node.js 20+, pnpm 8+)
  - [ ] Document installation steps
  - [ ] Document development commands
  - [ ] Document project structure overview

- [ ] **Task 8: Add Environment Configuration** (AC: 5)
  - [ ] Create `.env.example` with PORT and other vars
  - [ ] Add `.env` to `.gitignore`
  - [ ] Document environment variables in README

- [ ] **Task 9: Write Unit Tests for Health Endpoint** (AC: 6)
  - [ ] Install Vitest and supertest in api package
  - [ ] Create `packages/api/tests/health.test.ts`
  - [ ] Test GET /api/health returns 200 with correct format
  - [ ] Test response includes status "ok" and valid timestamp

## Dev Notes

### Project Structure Reference
[Source: architecture.md#Project Structure]

The monorepo uses pnpm workspaces with three packages:
```
restaurant-flow/
├── packages/
│   ├── api/                    # Backend application
│   │   ├── src/
│   │   │   ├── index.ts        # Entry point
│   │   │   ├── app.ts          # Express app setup
│   │   │   ├── config/         # Configuration
│   │   │   │   └── index.ts    # Environment config
│   │   │   ├── routes/         # REST API routes
│   │   │   │   ├── index.ts    # Route aggregator
│   │   │   │   └── health.ts
│   │   │   ├── middleware/     # Express middleware
│   │   │   │   └── error-handler.ts
│   │   │   └── utils/          
│   │   │       └── logger.ts
│   │   ├── tests/              # Backend tests
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── web/                    # Frontend application (scaffolded only)
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── shared/                 # Shared types and utilities
│       ├── src/
│       │   ├── types/          
│       │   └── index.ts
│       ├── package.json
│       └── tsconfig.json
├── package.json                # Root workspace config
├── pnpm-workspace.yaml         # pnpm workspace config
├── tsconfig.base.json          # Base TS config
├── .eslintrc.js
├── .prettierrc
├── .env.example
└── README.md
```

### Tech Stack for This Story
[Source: architecture.md#Tech Stack]

| Technology | Version | Purpose |
|------------|---------|---------|
| TypeScript | 5.3+ | Type-safe development |
| Express | 4.18+ | HTTP server and routing |
| pnpm workspaces | 8.0+ | Package management |
| Vitest | 1.0+ | Unit testing |
| ESLint | Latest | Code linting |
| Prettier | Latest | Code formatting |

### Health Endpoint Specification
[Source: architecture.md#REST API Specification]

- **Endpoint:** `GET /api/health`
- **Response Format:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-13T12:00:00.000Z"
}
```

### Error Handler Pattern
[Source: architecture.md#Backend Architecture]

```typescript
// packages/api/src/middleware/error-handler.ts
export function errorHandler(
  error: Error,
  req: Request,
  res: Response,
  next: NextFunction
) {
  console.error('Error:', error);
  
  res.status(500).json({
    error: {
      code: 'INTERNAL_ERROR',
      message: 'An unexpected error occurred',
    },
  });
}
```

### Express App Setup Pattern
[Source: architecture.md#Backend Architecture]

```typescript
// packages/api/src/app.ts
import express from 'express';
import cors from 'cors';
import { errorHandler } from './middleware/error-handler';

export function createApp() {
  const app = express();
  
  app.use(cors());
  app.use(express.json());
  
  // Routes
  app.use('/api', createRoutes());
  
  // Error handling (must be last)
  app.use(errorHandler);
  
  return app;
}
```

### Environment Configuration
[Source: architecture.md#Deployment Configuration]

```bash
# .env.example
PORT=3001
NODE_ENV=development
```

## Testing

### Testing Standards
[Source: architecture.md#Testing Strategy]

- **Framework:** Vitest 1.0+
- **Test Location:** `packages/api/tests/`
- **Integration Testing:** Use supertest for API endpoint tests

### Test Pattern for Health Endpoint

```typescript
// packages/api/tests/health.test.ts
import { describe, it, expect } from 'vitest';
import request from 'supertest';
import { createApp } from '../src/app';

describe('GET /api/health', () => {
  it('should return 200 with status ok', async () => {
    const app = createApp();
    const response = await request(app).get('/api/health');
    
    expect(response.status).toBe(200);
    expect(response.body.status).toBe('ok');
    expect(response.body.timestamp).toBeDefined();
  });
});
```

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-13 | 0.1 | Initial story draft | Bob (SM) |
